<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ShoppingList.ContentViews.CategoryContentView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <VerticalStackLayout>
        <Grid Padding="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label x:Name="CategoryLabel" Grid.Column="0" FontAttributes="Bold" VerticalOptions="Center" />
            <Button x:Name="ToggleButton" Grid.Column="1" Text="▲" Clicked="OnToggleClicked" />
        </Grid>

        <!-- Open by default -->
        <StackLayout x:Name="ItemsContainer" IsVisible="True" Padding="4" />
    </VerticalStackLayout>
</ContentView>